Enable: true
Host: 127.0.0.1
Port: 2333
LoginServerName: "lobby"
AuthKey: ""
# 未登录玩家允许执行的额外指令（不区分大小写，不带 /）。
# 插件自带的 login/l, register/reg, bindemail/bdmail, resetpassword/repw,
# changepassword/changepw 已内置在默认白名单中，无需重复添加。
# 此处可添加其他需要在登录前允许的指令。
CommandWhitelist: []
